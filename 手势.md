# 语音戒指手势说明

## 1. 手势识别概览

戒指在手势模式下使用设备端 HMM（Hidden Markov Model，隐马尔可夫模型）识别动作。

识别模型运行在戒指固件内部，Python SDK 和 PC Web 页面只负责接收、解析和展示识别结果。当前仓库不包含 HMM 的模型参数、训练数据或固件实现，因此无法从 SDK 侧确认模型状态数、特征提取方式和识别阈值。

## 2. 手势 ID

`0x0702` 手势事件中的 `gesture_id` 定义如下：

| ID | 名称 | 含义 |
| ---: | --- | --- |
| `0` | `idle` | 静止或未识别到有效手势 |
| `1` | `rotate_back` | 向后旋转 |
| `2` | `rotate_front` | 向前旋转 |
| `3` | `wave` | 挥手 |

设备通常只在识别到有效手势时上报 `1`、`2`、`3`；`0` 一般不会主动上报。

## 3. 识别流程

```text
设备进入手势模式
  -> 本地 IMU 开始采集
  -> 用户长按戒指
  -> 用户完成旋转或挥手动作并松开
  -> 固件补采动作尾部数据
  -> 戒指端 HMM 识别
  -> 通过 0x0702 上报 gesture_id
  -> PC 或手机显示识别结果
```

进入手势模式依靠戒指按键操作完成。当前协议没有查询当前模式或主动切换模式的命令。

`0x0702` HMM 识别不要求应用先开启 `0x0605` 实时六轴上报。只有应用还需要通过 BLE 接收实时 IMU 数据时，才需要额外发送 `0x0601`。

## 4. `0x0702` 数据格式

协议包头和 CRC 按语音戒指 v4 协议解析。`0x0702` 包体为：

```text
timestamp_ms: u32
gesture_id:   u8
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `timestamp_ms` | `u32` | 设备侧事件时间戳，单位毫秒，不是 PC 当前时间 |
| `gesture_id` | `u8` | 手势编号，使用上面的 ID 表解释 |

Python SDK 示例：

```python
import ring_sound as sdk

event = await sdk.wait_sensor_gesture_event(ring, timeout_s=30.0)
print(event.timestamp_ms)
print(event.gesture_id)
print(sdk.sensor_gesture_name(event.gesture_id))
```

浏览器 SDK 示例：

```javascript
const event = parseGestureEvent(packet.body);
console.log(event.timestampMs, event.gestureId, event.gestureName);
```

## 5. 与 `0x0605` 六轴数据的区别

`0x0702` 和 `0x0605` 是两条不同的数据链路：

| 命令 | 内容 | 产生位置 | 是否需要 `0x0601` |
| --- | --- | --- | --- |
| `0x0605` | 批量加速度计和陀螺仪数据 | 设备 IMU 实时上报 | 需要 |
| `0x0702` | HMM 手势识别结果 | 设备固件内部算法 | 不需要 |

`0x0605` 中的 `accel_x/y/z` 和 `gyro_x/y/z` 是六轴原始数据，用于观察运动过程；`0x0702` 只返回最终的手势类别，不返回模型中间结果或动作轨迹。

## 6. 当前 PC 页面展示

PC Web 测试页应显示：

- 向后旋转次数和最近一次向后旋转
- 向前旋转次数和最近一次向前旋转
- 挥手次数和最近一次挥手
- 最近一次普通双击、按键双击、按键单击和 HMM 手势
- `gesture_id`、设备时间戳和 PC 接收时间

高频 `0x0605` 六轴帧不应混入戒指动作事件列表，否则会掩盖真正的用户操作。

## 7. 维护检查项

固件或模型更新后，需要重新确认：

1. `gesture_id` 是否仍为 `0` 至 `3`。
2. `rotate_back` 和 `rotate_front` 的物理方向是否发生变化。
3. `wave` 的动作定义和触发条件是否变化。
4. HMM 是否仍在设备端运行。
5. 长按时长、动作采集窗口和松开后的尾部补采时长。
6. 手势模式与录音模式的切换方式。
7. `0x0702` 包体是否仍为 `u32 timestamp_ms + u8 gesture_id`。
8. 未识别动作是否继续保持静默，还是开始上报 `gesture_id=0`。

## 8. 不修改固件的软件手势识别

PC Web 页面可以直接使用 `0x0605` 六轴数据，在浏览器中识别上下左右挥动，不需要修改或烧录戒指固件。

该功能属于本地软件识别，与设备端 `0x0702` HMM 结果分开：

```text
0x0605 六轴样本
  -> 静止姿态校准
  -> 去除加速度和陀螺仪基线
  -> 检测运动开始和重新静止
  -> 比较水平轴和垂直轴的主运动峰值
  -> 输出 swipe_up / swipe_down / swipe_left / swipe_right
```

页面默认方向映射：

| 软件手势 | 默认传感器方向 |
| --- | --- |
| `swipe_up` | `+Y` |
| `swipe_down` | `-Y` |
| `swipe_right` | `+X` |
| `swipe_left` | `-X` |

由于戒指安装方向和佩戴姿态可能不同，页面允许修改“哪个传感器轴和正负方向代表向上、向右”。修改映射后，应重新测试四个方向。

使用顺序：

1. 将戒指切换到手势模式。
2. 在页面开启 IMU，使设备开始上报 `0x0605`。
3. 保持手和戒指静止，点击“校准静止姿态”。
4. 校准完成后启用“软件手势识别”。
5. 在校准姿态附近完成上下左右挥动。

软件手势事件在页面中标记为 `LOCAL`，不会伪装成戒指固件上报的 `0x0702`。

静止姿态校准成功后，页面也会在“用户动作记录”中增加一条 `LOCAL` 事件，包含校准样本数、设备时间、加速度基线和陀螺仪基线。校准未完成或连接中断时不记录成功事件。

该实现当前是规则识别器，主要用于数据采集和功能验证。识别准确率会受到佩戴方向、动作幅度、速度和普通手部活动影响。长期使用前应采集真实用户数据，调整运动阈值、方向映射、动作窗口和拒识条件。

相关协议和 SDK 文档：

- `protocol.md`：`0x0701` 至 `0x0704` 命令定义。
- `ring_sound_use.md`：Python SDK 手势事件 API。
- `README.md`：设备模式、IMU 和手势行为说明。
